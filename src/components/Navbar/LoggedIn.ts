import { NavLink } from "./type";

const navLinks: NavLink = [
  { id: 1, title: "Home", path: "/" },
  { id: 2, title: "Create Writing", path: "/writing/create" },
];

export const LoggedIn = navLinks;
